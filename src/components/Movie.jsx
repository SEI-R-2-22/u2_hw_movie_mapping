import React from 'react'
 
const Movie = () => {

return (
  <div>
  {movieArray.map((movie, index) => (
        <div className="movieArray">
          <Movies />
        </div>
  ))}
</div>
)
 };

export default Movie;
